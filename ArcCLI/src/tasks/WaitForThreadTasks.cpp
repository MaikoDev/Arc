#include "WaitForThreadTasks.h"

namespace MaikoDev {
    namespace Arc {
        namespace Tasks {
            /*WaitForThreadTasks::WaitForThreadTasks(const MT::TaskPtr& task) {
                _tasks
            }

            WaitForThreadTasks::WaitForThreadTasks(const std::vector<MT::TaskPtr>& tasks) {

            }

            void WaitForThreadTasks::onRun() {

            }*/
        }
    }
}