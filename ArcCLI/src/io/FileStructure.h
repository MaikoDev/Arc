#pragma once

#include <objects/TrackedObject.h>

namespace MaikoDev {
    namespace Arc {
        namespace IO {
            class FileStructure {
            public:
                FileStructure();

                void init();
            private:

            };
        }
    }
}