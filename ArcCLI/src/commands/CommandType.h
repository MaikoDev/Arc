#pragma once

namespace MaikoDev {
    namespace Arc {
        namespace Commands {
            enum class CommandType {
                Status,
                Fetch,
                Pull,
                Add,
                Commit,
            };
        }
    }
}